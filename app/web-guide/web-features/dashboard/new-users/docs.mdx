import { DocLayout, DocImage } from '../../../../../components';

# New Users

This metric shows how many first-time visitors your website receives within a selected timeframe. Each unique user is counted once based on browser and device identifiers.

Tracking new users helps you measure growth and the effectiveness of marketing campaigns or SEO initiatives.

<DocImage src="/docs/web-new-users.png" width="1018" height="392" size="large" />

export default ({ children }) => <DocLayout current="web-new-users" previous={{href: '/web-guide/web-features/dashboard/session-time', label: 'Session Time'}} next={{href: '/web-guide/web-features/dashboard/total-sessions', label: 'Total Sessions'}}>{children}</DocLayout>